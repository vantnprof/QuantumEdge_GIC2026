# QuantumEdge Output Contract

The agent should preserve:

- All CSV and JSON files in `results/`
- All PNG files in `figures/` and `dashboard_figures/`
- Executed notebook copies
- `results/package_verification.csv`
- `results/package_verification_summary.json`

A final package is acceptable only when the strict verifier reports zero failures.
